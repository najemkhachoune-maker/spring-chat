er/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

     - name: Build and push Docker image
        run: |
         docker build -t ${{ secrets.DOCKER_USERNAME }}/chat-app-backend:latest .
         docker push ${{ secrets.DOCKER_USERNAME }}/chat-app-backend:latest

      # 🔍 Semgrep scan
      - name: Run Semgrep
        uses: returntocorp/semgrep-action@v1
        with:
          config: "p/ci"

      # 🔍 Trivy scan (Docker image vulnérabilités)
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: najemkub/chat-app-backend:latest
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
